# 🚀 网站部署指南

## 📦 部署前检查清单

✅ 确保以下文件都在项目中：
- `index.html` (首页)
- `chat.html` (AI对话页面)
- `guestbook.html` (留言页面)
- `styles.css` (样式文件)
- `script.js` (首页脚本)
- `chat.js` (AI对话脚本)
- `guestbook.js` (留言板脚本)
- `social-links.js` (社交媒体链接)
- `fix-paths.js` (路径自动修复)
- `images/` 文件夹（包含所有图片）
- `robots.txt` (搜索引擎爬虫规则)
- `sitemap.xml` (网站地图)
- `vercel.json` (Vercel配置)
- `.gitignore` (Git忽略文件)

## 🌐 GitHub Pages 部署

### 方法1：使用Git命令行

1. **初始化Git仓库**（如果还没有）
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **创建GitHub仓库**
   - 登录 GitHub
   - 点击右上角 "+" → "New repository"
   - 仓库名：例如 `ilovehzl-website`
   - 设为 Public（免费版必须）
   - 不要勾选 "Initialize with README"

3. **推送代码到GitHub**
   ```bash
   git remote add origin https://github.com/你的用户名/ilovehzl-website.git
   git branch -M main
   git push -u origin main
   ```

4. **启用GitHub Pages**
   - 进入仓库 Settings
   - 左侧菜单找到 "Pages"
   - Source 选择 "Deploy from a branch"
   - Branch 选择 "main" 或 "master"
   - Folder 选择 "/ (root)"
   - 点击 Save

5. **访问网站**
   - 等待几分钟后访问：`https://你的用户名.github.io/ilovehzl-website/`

### 方法2：直接上传文件

1. 在GitHub创建新仓库
2. 点击 "uploading an existing file"
3. 将所有文件拖拽上传
4. 提交更改
5. 在Settings → Pages启用GitHub Pages

## ⚡ Vercel 部署

### 方法1：通过GitHub导入

1. **登录Vercel**
   - 访问 https://vercel.com
   - 使用GitHub账号登录

2. **导入项目**
   - 点击 "New Project"
   - 选择你的GitHub仓库
   - 点击 "Import"

3. **配置部署**
   - Framework Preset: 选择 "Other"
   - Root Directory: `./`
   - Build Command: 留空（静态网站不需要构建）
   - Output Directory: `./`
   - Install Command: 留空

4. **部署**
   - 点击 "Deploy"
   - 等待部署完成

5. **访问网站**
   - 会获得一个类似 `your-project.vercel.app` 的地址

### 方法2：使用Vercel CLI

```bash
# 安装Vercel CLI
npm i -g vercel

# 在项目目录运行
vercel

# 按照提示操作
```

## 🔧 常见问题解决

### 问题1：图片不显示
**原因**：路径问题
**解决**：
- 确保所有图片都在 `images/` 文件夹中
- 确保HTML中使用的是 `./images/photo1.jpg` 格式
- `fix-paths.js` 会自动修复路径问题

### 问题2：页面跳转404
**原因**：链接路径问题
**解决**：
- 确保所有HTML链接使用 `./index.html` 格式
- `fix-paths.js` 会自动修复

### 问题3：GitHub Pages显示空白
**原因**：可能是仓库名或路径问题
**解决**：
- 检查仓库名中是否有特殊字符
- 确保 `index.html` 在根目录
- 检查 `fix-paths.js` 是否正确加载

### 问题4：CSS样式不生效
**原因**：CSS文件路径问题
**解决**：
- 确保 `<link rel="stylesheet" href="./styles.css">` 使用 `./` 前缀
- 检查浏览器控制台（F12）是否有404错误

## 📝 优化建议

1. **图片优化**
   - 图片文件可能较大，建议压缩后再上传
   - 可以使用在线工具如 TinyPNG 压缩

2. **SEO优化**
   - 已添加 meta 标签
   - 已添加 sitemap.xml 和 robots.txt
   - 如需自定义域名，记得更新 sitemap.xml 中的URL

3. **自定义域名**
   - GitHub Pages: Settings → Pages → Custom domain
   - Vercel: Project Settings → Domains → Add Domain

## ✅ 部署后检查

- [ ] 首页正常显示
- [ ] 图片轮播正常工作
- [ ] 页面跳转正常
- [ ] AI对话页面正常
- [ ] 留言板功能正常
- [ ] 社交媒体链接正常
- [ ] 移动端显示正常（响应式）

## 🎉 完成！

部署成功后，你的网站就可以通过URL访问了！

**GitHub Pages URL格式**：
`https://用户名.github.io/仓库名/`

**Vercel URL格式**：
`https://项目名.vercel.app`

如需绑定自定义域名 `ilovehzl.com`，请参考 `ILOVEHZL_DEPLOY.md` 文件。


